export type Appointment = {
    id: number;
    title: string;
    date: string;
    // Add other fields as necessary
  };

  export type AppointmentDetails = {
    id: number;
    title: string;
    description: string;
    date: string;
    time: string;
  };
